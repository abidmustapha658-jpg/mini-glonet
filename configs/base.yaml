seed: 42

data:
  num_steps: 900
  height: 48
  width: 64
  history: 4
  train_fraction: 0.8
  batch_size: 32
  num_workers: 0

model:
  hidden_channels: 32

training:
  epochs: 12
  learning_rate: 0.001
  weight_decay: 0.00001
  output_dir: outputs

evaluation:
  num_examples: 4
